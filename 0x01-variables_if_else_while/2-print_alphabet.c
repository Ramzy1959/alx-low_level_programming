#include <stdio.h>
/**
 * main - Entry Point
 * Description: 'A program that alphabet in lower case'
 * Return: Always 0 (success)
 */
int main(void)
{
	char alph[26] = "abcdefghijklmnopqrstuvwxyz'
	int i;
	
	for (i = 0; i < 26, i++)
	{
	putchar(alph[i]);
	}
	putchar('\n');
	return (0);
}

