#include <stdio.h>
/**
 * main - Entry Point
 * Description: 'A program that alphabet in lower case'
 * Return: Always 0 (success)
 */
int main(void)
{
	char 'c';

	for (c = 'a'; c <= 'z', ++c)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}

