#include <stdio.h>
/**
 * main - Entry Point
 * Description: 'A program that alphabet in lower case'
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 97; i <= 122; i++)
	{
	putchar(i);
	}
	putchar('\n');

	return (0);

}
