#include <stdio.h>
/**
 * main - Entry Point
 * Description: print int from number Zero to Ten
 * Return: Always 0 (success)
 */
int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
	{
	printf("%d\n", y);
	}
	return (0);
}
